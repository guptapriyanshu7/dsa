#include <iostream>
#include <unordered_map>

using namespace std;

int solve(vector<int>& nums) {
  for (int i = 0; i < nums.size(); i++) {
    cout << i;
  }
}

int main() {
  vector<int> nums{};
  solve(nums);
  return 0;
}