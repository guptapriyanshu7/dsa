#include <bits/stdc++.h>

using namespace std;

int solve(vector<int>& nums) {
  if (1) return 1;
  else cout << "fdafsdf";
  for (int i = 0; i < nums.size(); i++) {
    cout << i;
  }
}

int main() {
  vector<int> nums{};
  solve(nums);
  cout << "afdaff hrllo world";
  return 0;
}