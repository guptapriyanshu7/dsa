#include <bits/stdc++.h>

using namespace std;

int solve(vector<int>& nums) {
  for (auto&& i: nums) {
    cout << i << " ";
  }
  return 0;
}

int main() {
  vector<int> nums{ 1, 2, 3 };
  solve(nums);
  return 0;
}
